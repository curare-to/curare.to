export default function HomePage() {
  return (
    <div className="mx-auto max-w-3xl px-4 py-16">
      <h1 className="text-3xl font-semibold tracking-tight">Nothing here yet.</h1>
      <p className="mt-4 max-w-prose text-ink-2">
        curare.to is being built in the open, one phase at a time. The plan —
        subreddits as curated schema events, posts as curated canonical events —
        is in the repository under <code className="font-mono text-sm">docs/</code>.
      </p>
    </div>
  )
}
